<div id="modal_{{ type }}_publish_{{ entity.id }}" class="modal fade" tabindex="-1" role="dialog" aria-hidden="true" aria-labelledby="modal_{{ type }}_publish_label">
	<div class="modal-dialog" role="document">
		<div class="modal-content">
			<div class="modal-header">
				<h5 class="modal-title" id="modal_{{ type }}_publish_label">
					{{- 'duo.admin.modal.publish.title' | trans -}}
				</h5>
				<button type="button" class="close" data-dismiss="modal" aria-label="{{ 'duo.admin.modal.global.close' | trans }}">
					<span aria-hidden="true">&times;</span>
				</button>
			</div>
			<div class="modal-body">
				{# TODO: add form with date/time input #}
				{#<p>Lorem ipsum dolor sit amet...</p>
				<form action="javascript:;">
					<div class="form-group">
						<label for="modal_publish_date" class="sr-only">
							Publication date
						</label>
						<div class="input-group">
							<input type="text" id="modal_publish_date" name="date" placeholder="dd-mm-yyyy" class="form-control datepicker" data-date-format="dd-mm-yyyy" data-date-start-date="+0d">
							<div class="input-group-addon">
								<i class="icon-calendar"></i>
							</div>
						</div>
					</div>
				</form>#}
			</div>
			<div class="modal-footer">
				<button type="button" class="btn btn-secondary" data-dismiss="modal">
					{{- 'duo.admin.modal.global.cancel' | trans -}}
				</button>
				<button type="button" class="btn btn-primary">
					{{- 'duo.admin.modal.global.ok' | trans -}}
				</button>
			</div>
		</div>
	</div>
</div>
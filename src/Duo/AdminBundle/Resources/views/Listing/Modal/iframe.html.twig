<div id="modal_iframe" data-type="{{ type }}" class="modal iframe fade" tabindex="-1" role="dialog" aria-hidden="true" aria-labelledby="modal_iframe_title">
	<div class="modal-dialog modal-lg" role="document">
		<div class="modal-content">
			<div class="modal-header">
				<h5 class="modal-title" id="modal_iframe_title">
					{{- ('duo_' ~ type ~ '.modal.title') | trans -}}
				</h5>
				<button type="button" class="close" data-dismiss="modal" aria-label="{{ 'duo_admin.modal.global.close' | trans }}">
					<span aria-hidden="true">&times;</span>
				</button>
			</div>
			<div class="modal-body">
				<iframe src="{{ path(routePrefix ~ '_index', app.request.query.all | merge ({ iframe: true })) }}" scrolling="auto" frameborder="0" width="100%" height="100%"></iframe>
			</div>
			<div class="modal-footer">
				<button type="button" class="btn btn-primary" data-dismiss="modal">
					{{- 'duo_admin.modal.global.close' | trans -}}
				</button>
			</div>
		</div>
	</div>
</div>

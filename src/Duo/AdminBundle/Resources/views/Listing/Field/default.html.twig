{%- set value = value | default(attribute(entity, field.property)) -%}
{%- if value is instanceof('\\DateTimeInterface') -%}
	{{- value | localizeddate('short', 'short', app.request.locale) -}}
{%- elseif value is bool -%}
	{{- (value ? 'duo_admin.type.bool.yes' : 'duo_admin.type.bool.no') | trans -}}
{%- else -%}
	{{- value -}}
{%- endif -%}

{%- for taxonomy in entity.taxonomies | slice(0, 3) -%}
	{{- taxonomy.translate(app.request.locale).name }}{% if not loop.last %}, {% endif -%}
{%- endfor %}{% if entity.taxonomies | length > 3 %} ...{% endif -%}